<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingHorizontal="@dimen/item_month_selector_root_pad_hor">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnLeftMonthSelector"
        style="@style/Only.IconButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/item_month_selector_btn_left_pad"
        app:icon="@drawable/ic_arrow_left"
        app:rippleColor="@color/grey_100" />

    <FrameLayout
        android:id="@+id/flBoxMonthSelector"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1">

        <com.google.android.material.chip.Chip
            android:id="@+id/chipDateMonthSelector"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textAppearance="@style/ButtonTextStyle"
            android:textColor="@color/grey_800"
            app:chipBackgroundColor="@color/grey_100"
            app:chipEndPadding="@dimen/item_month_selector_chip_date_pad_end"
            app:chipIcon="@drawable/ic_calendar"
            app:chipIconSize="@dimen/item_month_selector_chip_date_icon_size"
            app:chipStartPadding="@dimen/item_month_selector_chip_date_start_pad"
            app:rippleColor="@color/blue_500"
            tools:text="May, 2021" />

    </FrameLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnRightMonthSelector"
        style="@style/Only.IconButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/item_month_selector_btn_right_pad"
        app:icon="@drawable/ic_arrow_right"
        app:rippleColor="@color/grey_100" />

</LinearLayout>