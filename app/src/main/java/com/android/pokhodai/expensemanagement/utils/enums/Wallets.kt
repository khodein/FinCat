package com.android.pokhodai.expensemanagement.utils.enums

import com.android.pokhodai.expensemanagement.R

enum class Wallets(val resId: Int){
    INCOME(R.string.status_income),
    EXPENSE(R.string.status_expenses)
}